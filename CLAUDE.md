# CLAUDE.md — note自動生成ルール

このリポジトリは、毎日1本の有料note記事を自動生成するためのものです。
GitHub Actions（毎朝10:00 JST、または手動の `workflow_dispatch`）から
`scripts/generate-note.sh` 経由で非対話的に呼び出されます。

人間の役割は「完成した記事を確認してnoteへ投稿すること」だけです。
noteへの自動投稿はこのリポジトリでは行いません。

## topics.md のステータス

`topics.md` はテーマ管理ファイルで、各テーマは以下の3ステータスのいずれかを持ちます。

```
[idea] = AIが作成したテーマ候補。実体験未確認。記事化禁止
[ ]    = 実体験確認済み。自動執筆可能
[x]    = 執筆済み
```

## 記事生成対象

- `[idea]` のテーマは絶対に使用しない
- `[x]` のテーマは使用しない（再利用しない）
- `[ ]` のうち、ファイル内で一番上にあるテーマを1件だけ使用する
- 1回の実行で複数テーマを処理しない
- `[ ]` が0件の場合は、ファイルを一切変更せず正常終了する

## 捏造禁止（最重要）

`topics.md` の該当テーマに記載されていない、以下の情報を新しく作らない。

- ユーザー自身の経験
- 成功体験・失敗体験
- 売上・PV・利用者数・導入実績
- 使用期間・日数・作業時間短縮の結果
- 検証結果・効果測定結果
- 第三者（読者・利用者）の感想

情報が不足している場合は、無理に補完せず、その情報なしで記事を成立させる。
テーマに書かれている事実情報だけを使用すること。

## 記事ルール

- 日本語で書く。noteにそのまま貼り付けられるMarkdownにする
- 文字数は2,500〜4,000文字程度を基本とする。情報量が足りない場合に水増ししない
- 無料部分は全体の30〜40%程度を目安にする
- 無料部分にも、読者が持ち帰れる実用的な情報を最低1つ入れる
- 有料部分には、具体的な手順・失敗・改善・テンプレート等、価値のある内容を入れる
- 有料部分の開始位置に、以下の文字列を**1回だけ**記載する

```
───ここから有料エリア───
```

- AI的な定型表現（「〜と言えるでしょう」「〜が重要です」「いかがでしたか」「まず第一に」「最後になりますが」等）を避ける
- 実際にやった本人が、同じことをやりたい人に共有する文章にする。専門家が上から教える口調にしない
- 結論から書き、一文を長くしすぎない。2〜4行程度で段落を分ける
- 同じ語尾を3回連続させない。同じ説明を繰り返さない

## 保存ルール

- 保存先: `drafts/YYYY-MM-DD-short-title.md`
- 日付は日本時間（Asia/Tokyo）の実行日を使用する
- 同じファイル名が既に存在する場合は上書きしない。末尾に連番等を付けた安全な別名で保存する

## topics.md 更新条件

以下をすべて満たした場合のみ、対象テーマのステータスを `[ ]` から `[x]` に変更する。

- Markdown記事ファイルの保存に成功している
- 本文が空でない
- 有料エリアの境界（`───ここから有料エリア───`）が正確に1回だけ存在する
- 捏造された経験・実績・数値が含まれていない
- 対象テーマ以外のステータスを変更していない

記事生成または保存に失敗した場合は、対象テーマを `[ ]` のまま残し、`topics.md` を変更しない。

生成結果は `scripts/validate-note.sh` によって機械的にも検証される。
検証に失敗した場合、生成した変更は破棄され、コミットされない。
</content>
